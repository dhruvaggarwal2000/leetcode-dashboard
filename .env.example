POSTGRES_USER=leetcode
POSTGRES_PASSWORD=changeme
POSTGRES_DB=leetcode_db

# Optional — only needed when app.chat.provider=api in application.yml.
# Leave blank if using the default CLI-based chat (claude -p).
ANTHROPIC_API_KEY=antropic-api-key
